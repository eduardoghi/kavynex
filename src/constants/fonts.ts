// The characterful display face, used with restraint. The wordmark and headings only. The body and
// UI stay on Inter (see the theme). Falls back to the UI stack if the bundled font fails to load.
export const DISPLAY_FONT_FAMILY =
    "'Bricolage Grotesque Variable', Inter, system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif";
